name: azure-stack

services:
  azurite:
    container_name: azurite
    image: mcr.microsoft.com/azure-storage/azurite
    command: ["azurite", "--blobHost", "0.0.0.0", "--queueHost", "0.0.0.0", "--tableHost", "0.0.0.0", "--location", "/workspace", "--skipApiVersionCheck"]
    ports:
      - "10000:10000" # Blob storage
      - "10001:10001" # Queue storage
      - "10002:10002" # Table storage
    restart: unless-stopped
    volumes:
      - ./volumes/azurite-data:/workspace
    networks:
      - azure-network

  cosmosdb:
    container_name: cosmosdb
    image: mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:vnext-preview
    ports:
      - "8081:8081" # CosmosDB service
      - "1234:1234" # CosmosDB explorer
    restart: unless-stopped
    volumes:
      - ./volumes/cosmosdb-data:/data
    networks:
      - azure-network
    environment:
      - PROTOCOL=https
      - DATA_PATH=/data
      - AZURE_COSMOS_EMULATOR_PARTITION_COUNT=10
      - AZURE_COSMOS_EMULATOR_ENABLE_DATA_PERSISTENCE=true

networks:
  azure-network:
    driver: bridge
