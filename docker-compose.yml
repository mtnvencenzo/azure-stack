name: azure-stack

services:
  azurite:
    container_name: azurite
    image: mcr.microsoft.com/azure-storage/azurite:latest
    command: ["azurite", "--blobHost", "0.0.0.0", "--queueHost", "0.0.0.0", "--tableHost", "0.0.0.0", "--location", "/workspace", "--skipApiVersionCheck"]
    ports:
      - "10000:10000" # Blob storage
      - "10001:10001" # Queue storage
      - "10002:10002" # Table storage
    restart: unless-stopped
    volumes:
      - "${HOME}/mnt/azure-stack/azurite-data:/workspace"
    networks:
      - azure-network

  cosmosdb:
    container_name: cosmosdb
    image: mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:vnext-preview
    ports:
      - "8081:8081" # CosmosDB service
      - "1234:1234" # CosmosDB explorer
    restart: unless-stopped
    volumes:
      - "${HOME}/mnt/azure-stack/cosmosdb-data:/data"
    networks:
      - azure-network
    environment:
      - PROTOCOL=https
      - DATA_PATH=/data
      - AZURE_COSMOS_EMULATOR_PARTITION_COUNT=10
      - AZURE_COSMOS_EMULATOR_ENABLE_DATA_PERSISTENCE=true

  eventhubs:
    container_name: eventhubs
    image: mcr.microsoft.com/azure-messaging/eventhubs-emulator:latest
    ports:
      - "5672:5672"   # AMQP
      - "9092:9092"   # Kafka protocol
      - "5300:5300"   # Health endpoint
    restart: unless-stopped
    networks:
      - azure-network
    environment:
      - ACCEPT_EULA=Y
      - BLOB_SERVER=azurite
      - METADATA_SERVER=azurite
    depends_on:
      - azurite

  servicebus:
    container_name: servicebus
    image: mcr.microsoft.com/azure-messaging/servicebus-emulator:latest
    ports:
      - "5671:5671"   # AMQP
    restart: unless-stopped
    networks:
      - azure-network
    environment:
      - ACCEPT_EULA=Y
      - SQL_SERVER=sqlserver
      - MSSQL_SA_PASSWORD=StrongPassword123!
      - SQL_WAIT_INTERVAL=60  # Time in seconds to wait for SQL to be ready (default is 15)
    depends_on:
      sqlserver:
        condition: service_healthy

  sqlserver:
    container_name: sqlserver
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "1434:1433"
    restart: unless-stopped
    networks:
      - azure-network
    volumes:
      - "${HOME}/mnt/azure-stack/sqlserver-data:/var/opt/mssql"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=StrongPassword123!
      - MSSQL_PID=Express
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P StrongPassword123! -C -Q 'SELECT 1' || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 30
      start_period: 60s

  appconfig:
    container_name: appconfig
    image: mcr.microsoft.com/azure-app-configuration/app-configuration-emulator:1.0.0-preview
    ports:
      - "8483:8483"
    environment:
      - Tenant:AnonymousAuthEnabled=true
      - Authentication:Anonymous:AnonymousUserRole=Owner
    volumes:
      - "${HOME}/mnt/azure-stack/appconfig-data:/app/.aace"
    restart: unless-stopped
    networks:
      - azure-network

networks:
  azure-network:
    driver: bridge
